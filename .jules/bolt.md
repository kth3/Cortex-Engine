## 2024-05-24 - Test assertions reflecting codebase
**Learning:** Do not blindly write tests that assert based on issue snippets. Always examine the current file source (e.g. `scripts/cortex/skeleton.py`) to see what logic is actually implemented and write asserts that match reality.
**Action:** Always manually `cat` or read source before writing tests and remove any temporary scratchpad files before submitting.
